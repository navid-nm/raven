if (args.length !== 1) {
   warn("Usage: check <filename>")
   process.exit(1)
}

xconst {
    fs = use("fs")
    ts = use("typescript")
    args = process.argv.slice(2)
    inputString = fs.readFileSync(args[0], "utf8")
    fileName = "virtual.js"
}
